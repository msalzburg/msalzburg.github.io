name: main

on:
  push:
    branches:
      - "master"

jobs:
  lint:
    uses: ./.github/workflows/lint.yml
  test:
    uses: ./.github/workflows/test.yml
  deploy:
    needs: [lint, test]
    uses: ./.github/workflows/deploy.yml
